#include<bits/stdc++.h>
using namespace std;

int main() {
  unordered_map<int, int> um;
  // similar to map it will be unique keys but it wont be sorted
  // count will work
  // remove will work
  // TC is constant for every action on set
  // in one in a billion case somes when the TC is O(n) when all the data falls in a same group
  return 0;
}
