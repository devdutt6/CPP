#include<bits/stdc++.h>
using namespace std;

int main() {
  multimap<int ,int> mm;
  // everything is same expect it stores multiple duplicate keys in sorted order
  // but access method mpp[0] syntax will not work
  return 0;
}