#include<bits/stdc++.h>
using namespace std;

int main() {
  // stores unique keys and does not store in sorted order
  // almost all the actions have TC of O(1) but in worst case 1 in mil scenario it goes to worst case O(n)
  // and lower_bound and upper bound does not work.
  unordered_set<int> us;
  return 0;
}