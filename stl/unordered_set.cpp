#include<bits/stdc++.h>
using namespace std;

int main() {
  unordered_set<int> us; // all the things are same as set like stores unique but only difference is does not store in sorted order
  // and lower_bound and upper bound does not work.
  return 0;
}